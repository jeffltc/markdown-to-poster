export const exportOptions = {
  toPNG: async (element: HTMLElement) => {/* ... */},
  toJPG: async (element: HTMLElement) => {/* ... */},
  toPDF: async (element: HTMLElement) => {/* ... */},
  toSVG: async (element: HTMLElement) => {/* ... */}
} 